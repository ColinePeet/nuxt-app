// requests related

export const httpRequest = async (method, url, data) => {

    try {
        const config = {
            method: method,
            url: url,
            headers: {},
            data: data
        }

        return await 
    }
}